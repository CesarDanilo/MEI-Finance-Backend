{
    "build": {
      "builder": "DOCKERFILE"
    },
    "deploy": {
      "preDeployCommand": "npx prisma generate && npx prisma migrate deploy",
      "startCommand": "node dist/server.js"
    }
  }